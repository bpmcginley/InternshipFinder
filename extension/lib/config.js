// Public build settings. Nothing secret belongs here: the Gemini key lives only in the Worker.
// See worker/API.md. Replace the placeholder once the Worker is deployed.
export const WORKER_URL = "https://internscout-api.example.workers.dev";
